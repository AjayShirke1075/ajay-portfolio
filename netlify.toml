[build]
  # No build command needed — this is a plain HTML/CSS/JS site
  command   = ""
  publish   = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options        = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy        = "strict-origin-when-cross-origin"
